# renewable-huber-native-cpu

Optional Rust/PyO3 CPU engine for
[`renewable-huber`](https://github.com/Funtrollor/renewable-huber).

This wheel contains only the compiled native extension. It requires a
compatible `renewable-huber` 0.6 release, which owns the public estimator API,
validation, and portable checkpoint format.
